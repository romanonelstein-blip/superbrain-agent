import { ApprovalEngine } from '../workflow/approval-engine';describe('ApprovalEngine', () => {const engine = new ApprovalEngine();describe('requiresApproval', () => {test('should return true for dangerous operations', () => {expect(engine.requiresApproval('push')).toBe(true);expect(engine.requiresApproval('merge')).toBe(true);});test('should return false for safe operations', () => {expect(engine.requiresApproval('status')).toBe(false);});});});