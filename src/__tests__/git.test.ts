import { GitProvider } from '../core/git';describe('GitProvider', () => {const provider = new GitProvider();describe('isRepository', () => {test('should return true for valid git repository', async () => {const result = await provider.isRepository('.');expect(typeof result).toBe('boolean');});test('should return false for invalid path', async () => {const result = await provider.isRepository('/tmp/nonexistent-repo-xyz-123');expect(result).toBe(false);});});});