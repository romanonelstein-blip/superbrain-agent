import { GitProvider } from './core/git.js';import { GitHubCliProvider } from './core/github.js';import { CapabilityRegistry } from './core/capability-registry.js';import { AuditLog } from './audit/audit-log.js';import { ApprovalEngine } from './workflow/approval-engine.js';export { GitProvider, GitHubCliProvider, CapabilityRegistry, AuditLog, ApprovalEngine };export * from './types/index.js';export async function initializeAgent(repositoryPath: string): Promise<void> {const git = new GitProvider();const github = new GitHubCliProvider();const capabilities = new CapabilityRegistry();const auditLog = new AuditLog(repositoryPath);const repoInfo = await git.getRepositoryInfo(repositoryPath);if (!repoInfo) throw new Error(`Invalid Git repository at ${repositoryPath}`);const ghStatus = await github.getAuthStatus();if (!ghStatus.isAvailable) console.warn('GitHub CLI not available:', ghStatus.error);const caps = await capabilities.getCapabilities();console.log('Available tools:');console.log(`Git: ${caps.git.version}`);console.log(`GitHub CLI: ${caps.githubCli.available ? caps.githubCli.version : 'Not available'}`);console.log(`Ollama: ${caps.ollama.available ? caps.ollama.version : 'Not available'}`);}