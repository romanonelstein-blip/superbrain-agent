import type { ActionResult } from '../types/index.js';export class ApprovalEngine {private approvedActions: Set<string> = new Set();private readonly DANGEROUS_OPERATIONS = ['push', 'merge', 'publish', 'delete', 'force-push', 'rebase', 'deploy', 'update-permissions', 'paid-service', 'external-message'];requiresApproval(operation: string): boolean {return this.DANGEROUS_OPERATIONS.some(dangerous => operation.toLowerCase().includes(dangerous));}async requestApproval(operationId: string, operation: string, reason: string): Promise<ActionResult> {if (!this.requiresApproval(operation)) {return {status: 'prepared',message: `Operation "${operation}" does not require approval`,details: { operationId },nextSteps: ['Proceed with operation'],risks: []};}return {status: 'approval_needed',message: `This operation requires approval: ${operation}`,details: { operationId, operation, reason, timestamp: new Date().toISOString() },nextSteps: ['Review operation', 'Confirm approval', 'Do NOT auto-fill credentials'],risks: ['Destructive operation', 'May affect repository', 'May expose sensitive data']};}}